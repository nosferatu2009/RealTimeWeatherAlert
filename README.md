# RealTimeWeatherAlert
A real-time data Weather streaming using:
- > Apache Kafka (via Docker)
-> Python Producer (simulates weather sensor events)
- > Python Consumer (processes weather data + triggers alerts)
- > Streamlit Real-Time Dashboard (charts + live metrics)
